# SaaS migration status

Updated 2026-08-28. This dirty worktree contains an unfinished migration and is not ready for production promotion. Existing product work and device data have not been reset.

## Verified

- Editor-runtime repair: the browser is now configured to load the exact installed Monaco pin from versioned deployment assets instead of the loader's older CDN default. Runtime rejection/20-second stalls yield an editable basic fallback with the existing draft/save/read-only protections. Unit checks pass; clean-build byte checks and real editor/diff/worker browser checks are added to CI and must pass before this checkpoint is accepted. See [runtime packaging, recovery and verification limits](./editor-runtime.md). No production deployment or paid resources are implied.

- Project-workspace projection repair: tests reproduced background tool output not restoring on return, prior-project terminal state leaking while switching, and a late activity-startup receipt overwriting a newly activated workspace. Account-lifetime projections now retain files/selection, terminal output/cursors, preview and VM status; hidden tools update only their origin, replacement/legacy preview events are fenced, and log readers reconnect from retained cursors. See [scope, tests and remaining live checks](./project-chat-lifecycle.md#project-scoped-workspace-projection). This is not a production deployment or durable manual-terminal-output archive; the hosted two-project generation/preview gate remains open.

- Project-chat controller checkpoint: navigation no longer resets/disposes hidden-project inactivity monitoring or loses local Stop/Retry state. Account-bound history/transport/Stop work aborts on disposal, deletion or account replacement. Stop waits for the SDK stream, then reconciles authoritative history instead of inventing a saved interruption; missing receipts require explicit reconnection and cannot launch duplicate generation. Stop now requires the generation request ID as well as message/project/account ownership, preventing delayed requests from stopping a newer Retry that reused the same assistant row. Real-SDK controlled-stream and component tests cover simultaneous projects, stale handlers, failed navigation, completion races, missing receipts, cancellation and provider failure; the database HTTP suite additionally verifies generation-fenced Stop. Full background terminal/preview restoration and live-provider/deployment validation are not implied. See [lifecycle protocol and remaining checks](./project-chat-lifecycle.md).
- Account-scoped sign-out checkpoint: six failing route cases reproduced missing account checks/JSON acknowledgment. The workspace now has confirmation, dirty-Monaco blocking, bounded pending/error/Retry states and late-response/navigation guards. The handler verifies the displayed account before local-session sign-out, denies stale accounts, preserves native 303/CSRF/GET behavior and returns private JSON receipts. All 34 new checks and 1,945 local tests pass (17 opt-ins skipped), plus lint, TypeScript, dependency audit and an isolated Node 24 webpack production build. The real two-user hosted HTTP suite and disposable-session browser confirmation/cancel/sign-out flow pass; test accounts and isolated build/server are cleaned up. Production, real OAuth/email delivery, isolated Preview and multi-tab refresh verification remain open. See [exact sign-out evidence and limits](./authentication-readiness.md#account-scoped-sign-out-2026-08-28).
- Hosted Auth security checkpoint: enabled included Pro leaked-password protection, confirmed its persistence and actual `weak_password`/`pwned` rejection, and cleared the remaining security-advisor warning. Added ten callback-scoped redirect entries for CodeTutor Studio and local ports 3010/3112 while preserving the existing five entries and `codetutor-app-red.vercel.app` default. The fresh 15-case service check passes, including encoded return destinations and rejected external/lookalike/path redirects; all three disposable probe accounts were signed out and removed. The shared default-site decision, SMTP/OAuth delivery, isolated Preview and app deployment remain open. See [hosted configuration evidence](./authentication-readiness.md#hosted-security-and-redirect-configuration-2026-08-28).
- Server-auth outage checkpoint: nine failing cases reproduced false signed-out responses, uncaught proxy failures, stale-claims redirect disagreement, interception of callback/sign-out, and unbounded authentication. Ten-second whole-operation deadlines, cancelled-client/late-cookie guards, authoritative sign-in return checks, structured 503/408 responses and private layout recovery now pass 108 focused checks and the complete 1,899-test suite (17 live opt-ins skipped). Lint, TypeScript, exact toolchain, dependency audit and the Node 24 build pass. After a disk-exhausted parallel run, only the project's generated Turbopack cache was removed and sequential reruns passed. Real two-user hosted HTTP/DB checks pass including sign-out routing/CSRF/cookie removal; both fixtures were cleaned up. Browser sign-in is clean. Google remains disabled and the existing password-protection advisor warning remains; provider delivery, isolated Preview and deployment are not verified. See [session outage handling and evidence](./authentication-readiness.md#server-session-outage-handling).
- Authentication-readiness checkpoint: the form now discovers enabled email/GitHub/Google methods, handles unavailable/malformed settings, bounds confirmation waits, rejects late OAuth redirects, prevents duplicate starts and supports explicit email resend. Safe activity/model destinations survive failed callbacks and signed-in resumption. Four initial failures were reproduced; all 109 focused checks and the 1,844-test suite pass (17 opt-ins skipped), plus lint, TypeScript, Node 24 build, toolchain, whitespace and dependency audit. The rebuilt sign-in page has clean browser logs, and the real two-user hosted HTTP/DB suite passes with fixture cleanup. Public Auth settings show email/GitHub enabled and Google disabled; the read-only preflight deliberately reports `GOOGLE_DISABLED`. No Preview branch exists; Google credentials, real email/OAuth delivery, password-protection settings and deployment remain open. No provider settings, schema or credentials changed. See [authentication readiness](./authentication-readiness.md).
- Verification-lifecycle checkpoint: reproduced and repaired unbounded provider/cleanup waits and stalled browser headers/body reads. Verification now has bounded complete-operation/AI/cleanup receipts, cancellation and late-stage guards, schema-validated capped AI output, SDK retries disabled, and matching BotID client/server protection for AI-only review. The client exposes Cancel and persistent reconciliation guidance; acknowledged scores survive progress-refresh failures. All 161 focused checks and the full 1,772-test suite pass (17 live opt-ins skipped), plus lint, TypeScript, Node 24 build, toolchain checks, whitespace checks and production dependency audit. The real two-user hosted database/API suite passes completed-score preservation, late-score denial and private failure-RPC checks; both disposable users were removed and synthetic cleanup jobs settled. No schema, credentials, paid AI/VMs or deployment changed. Successful live AI review, deployed BotID and signed-in visual cancellation remain open. See [verification deadlines and cancellation](./submission-history.md#verification-deadlines-and-cancellation-2026-08-28).
- Custom-generation checkpoint: reproduced and repaired Cancel leaving requests alive and account changes allowing late activity navigation. Complete-response deadlines, stale-result rejection, explicit saved-activity reconciliation, request deduplication, bounded provider output and matching BotID client/server checks now pass 46 focused tests. The full suite passes 1,749 tests (17 live opt-ins skipped), lint, TypeScript, Node 24 build, whitespace checks and production dependency audit. The real two-user hosted database/API suite also passes, including malformed-generation rejection before paid work, ownership, saved-source recovery, reservations and cascades. Its database-only fixtures now settle their own never-launched cleanup tombstones before deletion dispatch; both test users were removed without changing production quota/cleanup rules. Successful live generation, deployed BotID and release authorization remain open. See [custom-generation evidence and limits](./custom-activity-generation.md).
- Navigation/accessibility checkpoint: the mobile drawer now uses a named focus-contained dialog; modified/same-route clicks preserve dirty drafts, project forms retain labels/autofocus, and project switches return focus after asynchronous history loading. Narrow workspace panes stay mounted while switching views. The full suite passes 1,706 tests (17 live opt-ins skipped), lint, TypeScript, Node 24 build, whitespace checks and production dependency audit. A disposable authenticated desktop browser session verified all seven routes, create/rename/switch, collapsed link names and clean browser warning/error output; account/session/project cleanup was confirmed. Phone/tablet, native keyboard, screen-reader and axe verification remain open. No deployment, schema or credentials changed. See [navigation evidence and limits](./navigation-accessibility.md).
- Automatic-diagnostics checkpoint: ordinary HTTP access logs no longer trigger paid error analysis; project/account-scoped cancellation, bounded requests, deduplication and explicit retry now have unit/component coverage. The exact BotID challenge namespace bypasses the page sign-in redirect without weakening workspace/API authorization; an actual signed-out challenge request changed from 307 to 200 JavaScript. The full suite passes 1,685 tests (17 live opt-ins skipped), lint, TypeScript, Node 24 build, whitespace checks and a clean production dependency audit. Real two-user HTTP/DB/VM tests cover benign error reports, ownership and expiration. Browser manual startup → server → preview/reconnect produces normal access logs with no analysis requests or browser warnings/errors. Successful provider diagnosis and deployed BotID verification remain open; no deployment, schema or credentials changed. See [automatic diagnostics and exact limits](./automatic-diagnostics.md).
- Owned-preview/manual-start checkpoint: authenticated preview resolution now validates project/sandbox ownership, exposed ports and SDK-derived origins, with read-only UI, cancellation, bounded requests and embedding headers. Manual **Create blank workspace** no longer dispatches an AI prompt; it creates an owned sandbox directly and restores existing source before attachment. The full local suite passes 1,603 tests (17 live tests skipped), lint, TypeScript, Node 24 build, whitespace checks and a clean production dependency audit. A separate real two-user HTTP/DB/VM preview test passes. Signed-in browser QA verifies manual creation, Monaco save, terminal → preview, explicit expiry, dialog dismissal/reopen and saved-source restoration; servers must be restarted afterward. No application deployment or credential change was made. See [owned preview verification and limits](./owned-preview.md).
- Project blueprint checkpoint: all six original Project IDs now have concrete browser/CLI/service contracts, four staged milestones, failing starters, unchanged visible checks, hints and reflection. Test-only implementations pass all four stages and full workflows; JavaScript, TypeScript and React production bundles build, and TypeScript is checked separately. All six workflows also pass on one disposable Linux Sandbox (98.5 seconds), with confirmed final shutdown. The instruction dialog is read-only and labels milestone evidence as self-reported; Project submissions remain AI assessed, not trusted deterministic scores. Compiler preparation now admits only exact curated Project ID/language matches and retains setup-failure cleanup. Final local gates pass 1,546 tests (16 live opt-ins skipped), the latest 157 focused checks, lint, TypeScript, the Node 24 production build, whitespace checks, production HTTP smoke checks and a clean production dependency audit. No database migration, credential change or application deployment was made. Live rubric quality, browser/axe/responsive checks and the wider service/release gates remain open. See [Project contracts and verification limits](./project-blueprints.md).
- Challenge checkpoint: all 18 original IDs now have concrete contracts, failing starters, examples, hints and reflection. Three code contracts across five language tracks (15 entries) use server-owned 24-case grading and retained private evidence; three React interaction tasks remain AI assessed. The full Linux Challenge matrix and DSA foundation/isolation regression suite passed in 230 seconds, including React builds and VM cleanup. The two-user Challenge HTTP → hosted database → owned VM test passed correct/incorrect grades, cross-user denial, immutable source, cancellation, post-expiry summaries/archives and no AI usage; temporary resources were cleaned up. Five reproduced input/backpressure failures are repaired with 64 KiB input admission and nonblocking stdin; all seven I/O regressions pass. Final local gates pass 1,520 tests (15 live opt-ins skipped), lint, TypeScript, Node 24 build, diff checks and a clean production dependency audit. Hosted Supabase remains healthy; the security advisor retains the existing leaked-password warning and intentional private-table policy notices. No migration, credentials or deployment changed. Six Project blueprints remain incomplete. See [Challenge contracts and verification limits](./challenges.md).
- Debug content checkpoint: all 12 original Debug IDs now contain reproducible behavioral bugs, exact contracts, regression assertions, progressive hints and diagnosis/reflection files. Minimal implementation repairs pass the unchanged checks locally and on one disposable Linux VM; both pinned-dependency React projects build and VM shutdown completed. Shared compiler preparation covers exact curated Practice/Debug ID-language pairs before attachment, with failure cleanup and mismatch tests. All 1,442 default tests pass (13 live opt-ins skipped), alongside lint, TypeScript, the Node 24 build, whitespace checks and a clean production dependency audit. Debug remains AI-assessed; 24 Challenge/Project entries still need substantive content/evaluator work. No database migration, credentials or deployment changed. See [Debug contracts and verification limits](./debug-lessons.md).
- Practice content checkpoint: all 18 existing Practice IDs now have distinct contracts, runnable visible checks, concept explanations, progressive hints and reflection files. The workspace shows complete instructions in an accessible read-only dialog. Java/C++ curated Practice startup prepares an allowlisted compiler before attachment and cleans up setup failures. All 18 starter/solution pairs pass locally and on one disposable Linux VM; all three pinned-dependency React projects build, and the VM stopped successfully. The full suite passes 1,403 tests (12 opt-in live suites skipped), lint, TypeScript, the Node 24 production build and a clean production dependency audit. Practice remains explicitly AI-assessed, not trusted deterministic grading; 36 other non-DSA entries still need content. No database migration, credential change or deployment was made. See [lesson contracts and verification limits](./practice-lessons.md).
- Durable grading-evidence checkpoint: hosted migrations `20260827213939_durable_grading_evidence.sql` and `20260827215054_grading_evidence_fk_index.sql` retain private exact cases/output, reserve report storage before execution, enforce immutable plans/results and require trusted scores to match retained outcomes. Hosted ownership/validation/score/quota tests pass with rolled-back fixtures. The two-user local HTTP → hosted DB → real VM suite passes correct/incorrect grading, immutable source, cancellation, expiry, safe history and checksum-verified exports; all disposable resources were cleaned up. The six newly reproduced submission-history header/body hangs are repaired with a full-read deadline and explicit retry. Final gates pass 1,355 local tests (11 opt-in suites skipped), lint, TypeScript, Node 24 build and a clean production dependency audit. All 39 local migration names/versions and generated types match hosted state; both new SQL hashes match exactly. The foreign-key index notice is resolved. The application is not deployed; exact historic runtime replay, full browser checks and the wider release gates remain open. See [evidence contract and limits](./grading-evidence.md).
- Interrupted-grading cleanup checkpoint: a real VM reproduced the leaked staged payload after successful process termination. New private ID-derived scratch directories, atomic registration, bounded serialized cleanup, pidfd exit confirmation, closed-run fencing and bounded/fair orphan/late-upload reclamation now pass 24 additional local regressions. The final suite passes 1,281 tests (11 opt-in tests skipped), lint, TypeScript, a Node 24 production build, diff checks and local production HTTP smoke checks; the dependency audit reports no known vulnerabilities. The expanded cancellation test passes real Stop, preserved student source, late dispatch, external SIGKILL recovery, stale-PID isolation and closed-but-live duplicate protection. Both all-language suites passed again (75 combinations plus runtime/isolation checks, 410 seconds total); every disposable VM stopped successfully. No AI call, schema migration, credential change or deployment was made. Orphan cleanup needs a later grading launch; historical unregistered scratch trees and deployed resource/load monitoring remain explicit limits. See [protocol and evidence](./grading-cleanup.md).
- Full DSA catalog checkpoint: all 15 existing problem IDs now have explicit contracts, five-language failing starters, and server-owned behavioral grading. The 12 newly completed tasks passed the real-VM matrix in all 60 language combinations: 24 solution checks and one failing starter check per variant, with staged payload removal and confirmed VM shutdown. Unit coverage compares 5,760 generated/boundary cases against independent implementations; route/runtime tests cover all 75 variants. The default suite passes 1,257 tests (11 opt-in live tests skipped), with lint, TypeScript, a Node 24 production build, whitespace checks and zero known production dependency advisories. HTTP smoke checks pass against the rebuilt local server on port 3112; the isolated helper's fixed port 3115 was already occupied and its listener was left untouched. No AI, database migration or deployment was involved. The 54 non-DSA catalog entries and the wider SaaS release gates remain unfinished. See [contracts and evidence](./trusted-grading.md).
- File-editor recovery follow-up: stalled reads now have a deadline spanning both headers and body, with explicit retry and late-result rejection. File/folder creation cancels when its sandbox expires, is replaced, or its owning account changes; late success cannot reset a newer draft or bypass the unsaved-file guard. Seven new regression tests passed after reproducing the failures. This is a local repair, not proof that the old production site's generic error boundary has been diagnosed or redeployed.
- The foundational DSA real-VM regression also passed again after the catalog extension: the original 15 activity/language combinations, immutable runtime/source, network/filesystem isolation, tampering/forged-score rejection, output limits and runaway-process cleanup. This brings the current live matrix to all 75 combinations. Both disposable VMs stopped successfully; no AI calls were made.
- Reproducible-CI checkpoint: all direct dependency versions are pinned to the previously verified graph; Node `24.18.0` and pnpm `11.19.0` are explicit. A fresh copy without existing dependencies/private env files passed frozen install, lint, route generation, TypeScript, 1,015 tests, production build and real loopback HTTP smoke checks. The checks-only GitHub workflow has read-only permissions and immutable actions; actionlint passes. Hosted transactional security assertions pass with full rollback. The Linux/Docker migration replay job is implemented but has **not run**, and no GitHub activation, branch-protection change, push or deployment was made. See [CI scope, evidence and activation](./continuous-integration.md).
- Durable startup/deletion cleanup checkpoint: hosted migration `20260827200351_durable_sandbox_cleanup.sql` retains deterministic VM handles across failed startup and project/account deletion. Unconfirmed shutdown retains project/account quotas; private leased jobs survive request loss and reject stale settlement/late attachment. Hosted protocol tests pass, including account cascades and the two-sandbox limit. A rebuilt local HTTP → hosted DB → two sequential real-VM test passes owner/CSRF checks, actual Next post-response cleanup and missing-creation-receipt recovery. Both VMs stopped; zero temporary accounts and zero pending cleanup jobs remain. Production build and dependency audit pass. The worker cron is checked in but not deployed; this is not a public-site repair or production promotion. See [cleanup protocol, tests and limits](./sandbox-cleanup.md).
- Activity startup follow-up: 988 tests pass (nine opt-in suites skipped), with lint, TypeScript, diff checks, a Node 24 production build and a clean production dependency audit. Startup now has account/project-scoped cancellation, complete file receipts, source-preserving retries, and language persistence before runtime preparation. Queued writes cannot borrow a replacement account. The language selector is usable inside the startup panel and locked while preparing; the covered workspace is inert. A real browser cancellation confirmed the disposable VM stopped; retry with Java successfully compiled and passed trusted grading, then recovered saved Java source after expiration. See [browser evidence and cleanup status](./browser-workspace-verification.md). These repairs remain local and do not complete the SaaS release gates below.
- Signed-in browser checkpoint: repaired real loopback-origin rejection, empty-chat history loading and a hardcoded activity submission label. The browser now creates a project, opens an activity, retains a Monaco draft across Preview, saves and earns 100% on 24 trusted checks, reads submission history, dismisses/reopens the expiry dialog, restores saved source into a replacement VM and runs that restored code successfully. Reload and idle-project switching preserve the correct source/progress. All 969 tests pass (nine opt-in suites skipped), alongside lint, TypeScript, Node 24 production build, diff checks and a clean production dependency audit. Disposable accounts/sessions/projects/VMs were cleaned up. Regression coverage includes real AI SDK history validation and real NextRequest origin normalization. See [the exact browser scope and local-only fixture](./browser-workspace-verification.md). This does not establish OAuth/email delivery, live AI streaming, embedded preview, full accessibility/responsive coverage or a production deployment.
- Trusted-grading checkpoint: 940 unit/component/route tests pass, nine opt-in live suites skipped by default. All 15 combinations of three foundational tasks (Two Sum, Valid Parentheses and Binary Search) and five languages pass real-VM behavioral grading. The authenticated HTTP suite proves frozen-source scoring, ownership/CSRF checks, no AI usage, interruption/expiry without scores and retained history. A reproduced privileged-supervisor cancellation failure is fixed and verified with process-tree cleanup, private stop records and stale-PID isolation. No schema change or deployment was made. See [scope, limits and exact tests](./trusted-grading.md).
- Earlier recovery checkpoint: 894 unit/component/route tests passed, with six opt-in live suites skipped by default. TypeScript, lint, a Node 24 production build and `git diff --check` passed; the production dependency audit reported zero known vulnerabilities. Earlier cache-compaction warnings and an `ENOSPC` rebuild were traced to exhausted local disk space; only verified generated caches were removed. Source, credentials and user data were not removed. The expired/stopping sandbox state cannot be revived by a delayed running response, and failed final-status reads stay recoverable without clearing source or drafts. These local results do not mean the old production deployment has been repaired.
- JSON body reads now have a ten-second deadline; file/snapshot reads have one twenty-second deadline across ownership, source lookup and streaming. Cancelled reads close acquired/late streams, strict snapshot validation rejects ambiguous writes, and receipts explicitly identify excluded files. The live local HTTP/hosted database/owned VM suite passes snapshot isolation, validation and expiration checks, with disposable resources cleaned up. No schema, customer source or deployment changed in this checkpoint. See [file API recovery limits and evidence](./file-api-recovery.md).
- Explicit saved-resolution application now passes a real local HTTP → hosted Supabase → owned Vercel VM integration. It covers actual conflicting terminal capture, owner isolation, malformed requests, background-command locks, exact Unicode application and retry, changed-terminal refusal, superseded saved revisions, deletion/journals and source readability after expiration. Both temporary accounts and the VM are cleaned up. This test caught missing request-ID/no-cache headers on file-save responses; all file-route responses now use consistent headers and regression tests pass. Draft-preserving editor refresh has component coverage, but signed-in browser verification and deployment remain outstanding. See [source consistency and the opt-in command](./source-consistency.md).

- Full archive recovery supports v2 and v3. New v3 exports combine current saved records and all earlier imported evidence in one flat archive, without replaying tools or restoring historical source over newer files. The missing-history regression was reproduced before repair. Four-generation database tests, two combined HTTP re-export/recovery cycles with disposable accounts, byte-for-byte comparisons, corruption/order checks, cross-owner denial, deletion and safe retries now pass. Migration `20260827173133_transitive_project_archives.sql` is applied and its exact SHA-256 matches hosted history; regenerated public types are unchanged. No new advisor warnings appeared. The original-file download is optional; the full archive now includes both histories. The app/cleanup remain undeployed. See [archive recovery and remaining limits](./archive-imports.md).

- Atomic source-only import now stages all files privately, validates hashes/counts/byte limits, then creates the project and complete revisioned source in one transaction. Retries preserve newer source; cancellation never deletes a published project. Hosted SQL tests include an exact 10 MiB escaped-source fixture, and two-user cookie-authenticated HTTP checks pass for ownership, concurrent publication, CSRF, malformed input and quotas. The dialog retains drafts, supports resume and marks imports as ungraded Playground projects. No imported tool history, scores or VM credentials gain authority. Migration `20260827164507_atomic_source_imports.sql` is applied; generated types and advisors are checked. The app/cleanup are not deployed, and legacy-device migration and signed-in visual checks remain open. See [source import protocol](./source-imports.md).
- Hosted Supabase schema and generated database types are checked in, including existing migration history.
- Cookie-based email/GitHub authentication, protected workspace pages, sign-out, and authenticated project CRUD are implemented.
- A real hosted integration test passes for two temporary users: cookie sessions, project persistence, cross-user isolation, source-file ownership/path constraints, CSRF, cascade deletion, and page protection. Temporary sessions are signed out and users deleted in `finally`.
- Run this test only against a local app: `node --env-file=.env.local scripts/verify-auth-projects.mjs`. It creates no sandboxes and sends no email or AI requests.
- 753 unit/component/route tests pass (four opt-in live VM tests are skipped by the default run), as do lint (no warnings), TypeScript, the production build and `git diff --check` on Node 24. The latest production dependency audit reports zero known vulnerabilities. The latest build also passed with the existing local cache; no source or user data was removed. These gates cover durable capture, fair scheduling/bounded retries, final-source shutdown, source conflict review/resolution, submission safety, complete archive export, byte-safe command output, restoration cancellation, AI service-failure handling and atomic source/archive imports; they do not establish production deployment, successful live AI generation, complete source synchronization.
- Full saved-project archive export now freezes source, all messages, conflict copies, submissions/files, scores and relevant activity/portfolio records in one database statement. Hosted checks pass with 1,105 messages, concurrent source saves, cross-user isolation, private RPCs, hashes and staging-only cleanup. A large JSON-escaped conflict fixture exposed an oversized record; splitting each preserved version into its own record fixed it without truncation. Cookie-authenticated HTTP and client cancellation/retry tests pass. Hosted migrations `20260827132501_project_archives.sql` and `20260827133209_archive_conflict_copy_records.sql` are applied. signed-in visual checks and deployment of export/import/cleanup remain outstanding. See [archive protocol and limits](./project-archives.md).
- Hosted learning-history checks pass with two temporary users: private generated activities, cursor pagination, server-only assessment writes, atomic project completion, idempotent assessment recording, RLS-safe progress aggregation, forged verification input rejection, and cascading deletion. No live AI or sandbox execution is claimed by these tests.
- Sandbox commands use a configured 60-second foreground process deadline, abortable wait/log requests, and streaming 64 KB output capture. Trusted DSA grading uses an additional registered privileged-supervisor stop path; SDK kill acknowledgment alone was insufficient for that process tree. Other activity assessment runs no commands, and no assessment trusts learner-owned test results. Cancelled/unavailable assessments retain interrupted or failed submissions without a fabricated score. Assessment rejects oversized or missing source instead of treating a bounded prefix as a complete solution.
- `@vercel/sandbox` is pinned to 3.1.0. New VM names remain exposed as the existing `sandboxId` field. Creation explicitly disables persistent VM snapshots; lookups use `resume: false`, and execution uses fixed `Session` handles to avoid automatic restarts bypassing quotas. Unit coverage verifies expiry, ownership, and uncertain-creation cleanup. Live local-app checks now verify creation, writes, commands, stop/410, replacement creation, saved-source restoration and successful execution after restoration.
- `scripts/verify-live-sandbox.mjs` passed against the rebuilt local app, real hosted Supabase and Vercel Sandbox: two-user command isolation, durable source, exact 125,000-byte Unicode output across cursor windows, small Unicode, inherited no-new-privileges/sudo denial, four concurrent starts yielding exactly three commands and one quota rejection, cross-user Stop denial, confirmed command Stop with cancelled audits, a delayed command reconnecting after 20 seconds, exit-code 3, final-source-preserving sandbox Stop, expiration, replacement and restore with exact output again. Both VMs were stopped and the two temporary users/project removed. This test does not call AI models, exercise the signed-in browser, or verify preview rendering. The deliberately raw upstream diagnostic remains separate from the repaired encoded command transport.
- Command output uses per-stream UTF-8 byte cursors, bounded NDJSON windows, one visible subscriber per command, finite reconnect retries, cancellation, and bounded browser log retention. Live tests exposed that detached command metadata can stay null until the SDK wait endpoint is consulted; a cancellable one-second completion probe now resolves completed commands without unbounded waiting. See [command output transport](./command-log-protocol.md).
- The Sandbox SDK's transitive `jsonlines` parser now has a checked-in pnpm patch for split UTF-8 network characters. Four tests exercise the actual parser; three failed before the patch. New commands additionally encode raw pipe bytes before the upstream text service, then decode validated frames incrementally before byte limits/cursors. A real VM test passes with 450,000 bytes of mixed-language output, two replays, nonzero exit, Stop and timeout. The app reproducer now receives the exact original 125,000 Unicode bytes across bounded windows. Historical raw logs can remain corrupted; the upstream diagnostic is retained. See [command transport](./command-log-protocol.md).
- Hosted migration `20260827134435_command_output_encoding.sql` adds a server-controlled output format without changing old commands or the existing attachment RPC. Hosted tests verify old/new attachment, cross-owner denial, denied browser writes/RPCs and attached-format fencing. Advisors report only the already documented notices. A subsequent live Stop check exposed one-shot capture dispatch losing a deferred claim; eight new dispatch tests cover bounded exact-job retry, terminal/deleted jobs and deadlines. The database protocol test proves an active account capture defers a shutdown while leaving it queued. The complete live app flow passes after this repair, and runtime logs show deferred jobs subsequently acknowledged. Deployed scheduler verification remains required.
- Hosted command reservations atomically enforce three active commands across all of a user's projects and 30 starts per rolling minute. Server-only RPCs own transitions; clients can read only their own audit metadata. Tests verify parallel requests, cross-user denial, immutable ownership, uncertain-launch slot retention, duplicate request IDs, late-completion fencing, rolling-window recovery, and cascades. Audits retain neither arguments nor output.
- Terminal, AI commands, and verification share this execution boundary. Command IDs require an owned audit lookup before status/log/Stop access. The SDK has a second checked-in patch preventing automatic retries of command-creation POSTs after uncertain failures; both installed ESM and CommonJS paths are tested. Safe GET retries are preserved.
- Live Stop testing exposed a real process-tree leak: killing a shell left its children and output pipes running. The command guard now uses unprivileged user/PID namespaces with a kernel parent-death signal. It retains the original UID/GID and drops capabilities before user code. A separate real-VM check verifies Stop and SDK timeouts terminate both normal and `setsid` children, checks zero effective/bounding capabilities and `NoNewPrivs`, and confirms Node/npm plus workspace writes still work. No model-controlled root execution is enabled. Run `node --env-file=.env.local scripts/diagnose-sandbox-stop.mjs`; the opt-in `REPRODUCE_UNGUARDED=1` path retains the original failing regression.
- Terminal Stop now preserves logs received while its request was pending, concurrent Stop buttons track their own operations, failed starts keep the input, and obsolete account/sandbox responses cannot update the new workspace. Seven component tests cover these behaviors.
- A VM-side shutdown guard closes command admission, terminates learner PID namespaces with stable kernel handles, waits for command/source locks and rejects late source writes without destroying source. Command dispatch retains its previous non-root PID/capability restrictions. The local public Stop API now durably reserves one final-capture job, returns 202 while saving, records quiescence before reconciliation, and stops only after complete source/conflicting-copy persistence. Hosted tests cover ownership/RLS, concurrent idempotency, lease fencing, twelve-failure/crash pauses, retries, incomplete scans, expiry and cascades. A live authenticated test passes through final-only file preservation, Stop, conflict review/acceptance and replacement restore/execution. Disposable VMs/accounts were cleaned up. The matching application/scheduler are not deployed; this is not a production guarantee. See [shutdown protocol](./sandbox-shutdown.md).
- Public tool errors no longer forward raw provider bodies, source arguments, or credentials. The production dependency audit reports zero advisories after the SDK migration.
- Vercel project `codetutor-studio` now has Node `24.x` configured, verified through the project API. This configuration change did not create a deployment or replace the live site.
- Workspace history reads now have bounded startup and a retry screen. Failed reads are not interpreted as empty accounts; no database clearing is used for recovery.
- Sandbox recovery validates saved files, batches uploads, attaches replacements only after acknowledgments, and cleans up failed replacements. Expiration is distinct from an application failure.
- Recovery component tests cover dismiss/reopen, failed restoration, cancellation, stale callbacks, Strict Mode file hydration, preserved drafts, and explicit retry after a source read failure. The activity workspace no longer starts a second, unscoped saved-file loader.
- Generated files, editor saves, new files, and snapshot uploads now commit authenticated, project-scoped source on the server before applying it to the VM. Database failure leaves the VM unchanged; failed VM application retains the saved source with explicit recovery guidance. Browser generation-snapshot callbacks and duplicate editor/starter saves were removed. Terminal changes use the separately verified capture worker below; production scheduling and applying reviewed resolutions to the VM remain unfinished.
- Nested file generation receives cancellation, stops when its consumer exits, and rejects incomplete requested file sets instead of reporting success. Tests verify per-file durable acknowledgments, database/VM failures, project isolation, create-only safety, and provider cleanup without live AI calls.
- Source files now have database revisions. The server-only `save_source_revision_batch` function checks ownership and all expected revisions in one transaction; direct authenticated/anonymous table mutations are revoked. Hosted tests prove exactly one of two competing stale writers succeeds, conflicting batches leave no partial writes, lost-receipt retries are idempotent, unsafe paths/revisions are rejected, and cross-user access is denied. The checked-in migration is applied to the hosted database, and generated types include the new fields/function.
- Editor loads carry a source revision; saves acknowledge only the submitted draft, preserving any newer typing. A conflict keeps the draft and requires an explicit Compare latest before another save. Fourteen component tests cover conflicts, retry, Strict Mode, malformed revisions/receipts, account changes, unmount and late file loads. Generation captures expected revisions before producing files rather than silently reading a fresh revision just before overwriting.
- Save receipts must identify the exact requested paths with valid revisions, not merely contain the right count. Duplicate or unrelated paths prevent VM application. The baseline and returned revisions are carried through source restoration.
- VM source application now has a protected revision journal, kernel-released file lock, digest-validated staging and atomic per-file replacement. It journals before writing, so an interrupted newer write cannot later be overwritten by an older request. A real VM test passes for delayed/concurrent writers, process death and retry, payload tampering, learner journal-access denial, symlinks, and 256 KB Unicode source. The live image uses `/vercel`; the writer reads `Session.cwd` instead of assuming a path. The fixed management program alone has root access; learner commands remain unprivileged. See [source consistency and remaining capture work](./source-consistency.md).

## Current recovery verification

- Expiry-dialog recheck (2026-08-28): production still keeps the old dialog open after Close; the reported full-page exception did not recur and no console errors appeared. Six new regression cases failed before repair: null/undefined render failures crashed the recovery boundary, primitive failures lacked a safe error classification, and Close/Not now/Escape lost keyboard focus. The local boundary now accepts unknown thrown values without dereferencing them; dismissal returns focus to the persistent Restore button. All 92 focused recovery checks and 1,850 default tests pass (17 live opt-ins skipped), along with lint, TypeScript, whitespace checks and the Node 24 production build. No browser storage, customer source, credentials, schema or live deployment changed. A recovery-only production release must be isolated from this unfinished SaaS migration; these results do not establish the cause of the original full-page exception.
- The 2026-08-28 recovery follow-up reproduced a cancellation hang: a replacement shutdown request that did not observe its abort signal left restoration pending indefinitely. Its best-effort cleanup now has an independent five-second wait, deduplicates shutdown requests, observes late responses, and logs unacknowledged shutdown using only the sandbox ID. This is not a rollback or a guarantee that the VM has stopped. No saved source or project association is changed on this failure path. The focused 55-test recovery suite and all 922 default tests pass (eight opt-in live suites skipped); TypeScript, lint, Node 24 production build, whitespace checks and production dependency audit pass. The live page opened without console errors in this check; the reported global crash was not reproduced. These changes remain local and do not establish a production fix.
- A stopped-before-mount recovery regression now has three additional passing component tests, reproduced as failures before repair: the panel reads final-save/conflict details even when the workspace already marks its sandbox stopped, surfaces failed checks inside the dialog with explicit Retry, and rejects stale running responses without reviving command access. Terminal reads settle instead of starting another polling loop. The full 753-test suite, lint, TypeScript and production build pass; this change is local, not deployed.
- The latest live browser check again reproduced the ineffective Close button, then restored the displayed saved project through the existing production Restore action. Its source editor and terminal became available and the dialog disappeared, with no browser console errors. The replacement was left available for the user; no browser storage was cleared, dependencies were not installed, and preview startup was not tested. The full-page load exception did not recur, so its original cause remains unconfirmed.
- Latest 2026-08-27 browser check: the live site still resolves to deployment `dpl_91SxHF2W9hMFcy9GoVeSp4V58LZy`. Close again left the old expiration dialog open. Invoking its existing Restore action successfully restored the displayed project's saved snapshot into a replacement sandbox: the dialog disappeared, source opened in the editor, and the terminal became available. The replacement was left available for the user; no browser storage was cleared. No console errors appeared, but the earlier global-load exception was not reproduced and no root cause is claimed for it. This live recovery used the existing production code, not a new deployment; dependencies/preview startup were not verified.
- Two additional status-reader regressions were reproduced and repaired locally: a cancelled check could still resolve as expired, and a stalled body reader could leave the check pending. Status reads now use the shared whole-operation deadline and bind the caller's cancellation to the account-scoped request, including the HTTP 410 branch. Both tests pass with the full 540-test suite, lint, TypeScript and production build. These changes do not modify APIs, stored source, credentials or database schema.
- Saved-work reads now propagate cancellation all the way to account-scoped fetch and pagination. Previously a timed-out history load, cancelled restoration, or unmounted file panel stopped waiting but left requests running; Retry could overlap them. Six new regression cases failed before repair and now pass. History failures also cancel their sibling request; retries use fresh signals without cancelling unrelated work for the account. Chat and saved-activity loaders share the deadline propagation. No API shape, saved source, database schema, or production deployment was changed by this follow-up.
- Recovery-panel rendering now has a project/sandbox-keyed local error boundary. The panel lives in the platform layout, outside the route error boundary; a failure there previously could reach the global application fallback. The new fallback keeps sibling editor/chat components mounted, permits explicit recovery retry, and never clears source or starts a replacement automatically. Regression tests verify draft preservation, recovery retry, project switching after failure, and failed status-check retry without an expiration loop. This is failure containment, not a claim to have reproduced the reported global exception. The 2026-08-27 live browser recheck still found the old dialog's Close action ineffective, with no console errors; these local changes have not been deployed.
- Restoration confirmation follow-up: four failing regressions reproduced indefinite waiting after successful source uploads, for both manual startup and restoration. Final project-save confirmation now has an independent 20-second deadline. An unknown result becomes `SandboxReopenRequiredError`, keeping the restored VM and offering Reopen project instead of another restore. Late success/failure cannot turn the timed-out operation into success or trigger cleanup. This bounds the confirmation wait, not the underlying queued write; no rollback is claimed. The live expiration dialog still refused Close during this check, while the application loaded without console errors. These repairs remain local; the separate reported global-load exception was not reproduced.
- The latest recovery follow-up reproduced two failing component cases before repair: switching to Preview unmounted the editor and discarded its draft, and a stalled restore had no Cancel action. Code now stays mounted but hidden during Preview. Restoration can be cancelled during reads/uploads, waits for cleanup before enabling Retry, and disables cancellation once the durable project association write starts. Tests also verify the commit-phase guard; no rollback of an already committed replacement is promised.
- A fresh production browser check again confirmed that clicking Close leaves the old expiration dialog open. The application itself loaded, and the browser reported no console errors during this check. The local dismiss/reopen, restoration retry, expired saved-file reads, draft-preservation, and startup timeout tests all pass in the full regression run. No browser storage was cleared and no production deployment was made.
- The latest follow-up reproduced six additional failing regression cases before repairing them: expiration unmounted the source editor and lost its draft, the editor did not support an expired read-only state, restoration did not confirm unsaved-draft loss, clicking the already selected file cleared the dirty guard, and saved-file reads unnecessarily depended on a live VM (including masking database failures with expiration). The mounted editor now remains read-only with its draft intact; restoration requires confirmation when a draft is dirty. Saved-source reads check authenticated registration/project ownership independently of VM liveness; command and mutation paths still require a live sandbox.
- The hosted two-user integration suite passes for expired-file reads through the rebuilt local app: the owner receives the saved content and revision, another account receives 404, anonymous reads receive 401, and writes still receive 410. The test creates no paid VM, sends no AI request/email, and signs out/deletes its two disposable users in cleanup. No production schema or customer data was changed by this follow-up.
- The local browser smoke check reached the expected sign-in screen without console errors. This is not an authenticated Monaco/restore browser check. Component tests cover those states; the global startup exception remains unconfirmed because it did not recur during the production reload.
- The production expiration dialog still did not dismiss when Close was clicked during the 2026-08-27 browser check. The local replacement dialog passes the dismiss/reopen regression test.
- The full “CodeTutor could not load” crash did not reproduce on that reload; no root cause is claimed for an unreproduced exception.
- Found and repaired a separate reproducible workspace-render crash: object-key file-tree lookup treated `constructor`, `__proto__` and similar file names as inherited properties, and a stale file entry followed by a child path dereferenced missing children. Eight new tests failed before replacing the lookup with Maps and deterministic folder reconciliation. Component tests verify those folders remain navigable and initial selection skips stale directory-as-file entries. This is a proven crash path, not proof that it caused the user's earlier production exception.
- Source restore now rejects file/directory path collisions before creating a VM. Activity manifests and source batches reject them too. Applied hosted migration `20260827101054_source_path_namespace.sql` adds the same check under the existing per-project database lock without editing any saved rows. The real hosted test reproduced the defect before migration and passes afterward: both batch orders roll back fully, concurrent conflicting writes have exactly one winner, API conflicts return 409, and literal/case-sensitive sibling names remain valid. The temporary users were signed out and deleted. The production UI is still the older deployment.
- Saved conversation/source reads have bounded, non-destructive recovery paths. An already attached sandbox no longer causes source-path hydration to be skipped.
- Confirmed and repaired an additional restore stall: authoritative project saves waited indefinitely if the optional IndexedDB cache was blocked. Cache writes now run without blocking the acknowledged server save, with late failures observed. Five regression tests cover blocked create/restore/delete, late cache failure, and failed authoritative saves; four reproduced the stall before the fix.
- Confirmed and repaired an account-change race in restoration: source loading, creation, uploads, status checks and cleanup now use the originating account's abort signal and account header. Tests reject delayed work after switching accounts, including cleanup that would otherwise run under the next account's cookies. A VM created just as its session is lost remains bounded by its server-side lifecycle; client cleanup never borrows another account's identity.
- These repairs are local. The live site still uses the older deployment. Do not promote the entire dirty worktree as a recovery-only patch: it also contains the unfinished authenticated SaaS migration below.
- The rebuilt local `/playground` route opened its expected sign-in screen in the browser without a visible application error. This signed-out check does not establish that authenticated workspace interactions or production recovery are verified.

## Durable capture verification

- Hosted migration `20260827130521_durable_sandbox_shutdown.sql` adds shutdown-purpose jobs without phony command audit records, complete/quiesced capture fencing and resumable Stop transitions. Generated database types are updated. UI tests verify dirty-draft blocking, confirmation, progress/retry, project-change cancellation and saved-versus-expired wording; stopping also cancels visible log subscriptions and error analysis. The complete local suite passes with 561 tests, lint, TypeScript and production build. Real database and VM/restore tests pass, but deployed scheduling and signed-in browser coverage remain release gates.
- Hosted migration `20260827123240_fair_source_capture_scheduling.sql` adds least-recently-served account scheduling, one active lease per account, bounded retry backoff and a twelve-failure/crash pause with retained acknowledgment checkpoints. The pre-migration two-project concurrency test reproduced two leases for one account; it now passes. Hosted database tests also verify cross-account progress, fairness against an older busy-account backlog, direct-dispatch enforcement, private grants/RLS, ten-job explicit retry limits and stopped-VM refusal. Nonempty acknowledgment replay preserves the exact committed file revision. No retention deletion is implemented or implied.
- The new owner-authorized Retry background saves action passes component and real cookie-authenticated HTTP tests for strict input, cross-user denial, CSRF, queued receipts, expiry, three-per-minute quotas, duplicate-click prevention and cancellation. The worker releases malformed but identified jobs through bounded retries and never settles a failed settlement twice. Test accounts/projects are removed in cleanup; no VM, email, AI call or app deployment was used for this follow-up. The post-migration security advisor has only the existing leaked-password-protection warning; new index notices are informational unused-index reports on the idle queue.
- Hosted migration `20260827105304_durable_source_capture.sql` adds atomic pre-command capture jobs, owner-only reads, service-only leasing/reconciliation, completion fencing and durable conflicting-copy preservation. The two-user hosted capture test passes, including deletion, retry and project cascades.
- Hosted migration `20260827110311_exclude_runtime_capture_files.sql` filters VM runtime caches, credential configuration and shell history out of source capture. It does not delete saved source or conflict rows. The real-VM test initially reported an incomplete scan from tool-home symlinks; regression tests reproduced this before the filter repair.
- The real local HTTP → hosted DB → disposable VM → worker → expired-file-read flow now passes. Terminal edits, creates and deletes become revisioned source; delayed completion is captured; concurrent editor/terminal versions are both preserved. The test uses the worker without browser command subscriptions, but does not prove a deployed scheduler. Its VM and temporary account are cleaned up in `finally`.
- The post-capture security advisor reports only the existing leaked-password-protection warning. Composite capture ownership indexes address the two new foreign-key performance notices. Existing unrelated unused-index/Auth-connection notices are not silently modified.
- See [source consistency](./source-consistency.md) for protocol details and remaining race/retention/deployment limits.
- Source conflict review/resolution is now implemented: project-scoped status, paginated reviews/history, saved/terminal comparison, manual merge and both-copy download. The hosted transaction tests verify exact-revision CAS, one winner for simultaneous choices, retained original/reviewed copies, idempotent retries, deletion and no recurrence for an exact reviewed capture. A real database test caught a PL/pgSQL variable ambiguity; the checked-in corrective migration repairs it and the suite passes.
- Authenticated HTTP integration checks now cover the new review endpoints with two users, including cross-user denial, malformed bodies, CSRF, pagination, stale revisions, merges/retries, retained copies and resolution after sandbox expiration. No paid VM/AI or email was used; test accounts were signed out and removed. Component tests cover merge drafts, confirmation, cancellation, error retry, copy download, keyboard labels and persistent saved-only guidance. No signed-in visual browser check is claimed.

## Activity submission safety checkpoint

- Submit no longer reads sandbox files into the browser and then writes them back as saved source. Explicit editor saves and server command-capture jobs retain their own revision checks. Unsaved drafts block Submit without discarding the draft.
- Verification requests carry the originating account and abort on activity/project replacement or unmount. Late responses cannot publish into a different workspace. Saved results remain visible if refreshing progress fails; the browser no longer sends a redundant project-completion write after the assessment transaction.
- Activity route changes remount activity-local state, and a failed automatic project creation presents Retry instead of an unhandled rejection. Resumed variants use the project's language rather than silently submitting the default template language.
- All 15 DSA activities in five languages and the 15 code-Challenge entries now use explicitly labeled Trusted checks; remaining activities use AI assessed review. A learner-controlled run/compile command earns no automatic points. The server copies every saved source file and its revision under the source transaction lock into immutable submission evidence, along with the activity/model/language/reflection. The assessment transaction compares live saved revisions against that frozen version before deciding whether the project itself is complete. Later edits/deletions and expired VMs do not change historical submitted files. Trusted execution requires a live prepared VM; historical evidence reads and AI-only review do not.
- Hosted migrations `20260827115426_immutable_activity_submissions` and `20260827121258_bound_submission_metadata_storage` are applied and generated types are checked in. Owner-only reads, service-only writes/RPCs, immutable content, composite ownership FKs, bounded evidence storage and count quotas are verified. Identical file contents deduplicate, but each repeated manifest is charged. Generated byte counts include JSON encoding and UTF-8. Exact 50 MiB project / 200 MiB account boundaries and concurrent final-slot claims pass hosted tests.
- Submission history now provides paginated attempts, frozen metadata, exact file/revision viewing and individual downloads. Reads are bounded and cancelled when obsolete; failed/interrupted attempts have no invented score. Real cookie-authenticated HTTP tests prove cross-user denial, immutable old files after edits, timestamp-tie pagination, invalid IDs/indexes, and expired-VM-independent source retention. Oversized evidence is retained without calling a provider. Test data is disposable and cleaned up; no AI, paid VM, email or application deployment was used for this checkpoint.
- All 39 local migration versions now correspond to hosted history, including durable shutdown, archive export/recovery and command output encoding, atomic source/archive imports, ownership indexes, published-import tombstone retention, transitive v3 archives, durable sandbox cleanup and private grading evidence. Two older filename timestamps were corrected only after exact SQL hash comparison; the remaining older byte-hash differences are trailing whitespace/empty statements, not SQL changes. Both grading-evidence migration hashes match exactly. No remote migration history was rewritten. A clean database replay in an isolated Preview environment is still required.
- This is **not the completed trusted-grading system**. All 15 DSA activities and 15 code-Challenge entries now have server-controlled behavioral checks. The 18 Practice lessons, 12 Debug exercises and three React Challenges have concrete content and editable checks but remain AI-assessed; six Project blueprints now also have concrete staged workflows and editable checks, with explicitly AI-assessed submissions. The AI-only reviewer accepts up to 64,000 UTF-8 bytes of complete JSON evidence and runs no commands; larger snapshots are retained without a score. Signed-in browser/live-provider checks, deployed cleanup monitoring, exact randomized-case replay and deployed submission-archive recovery remain release work. See [submission protocol](./submission-history.md) and [trusted grading](./trusted-grading.md).

## Release blockers

- Durable worker-invocation health is implemented on the release branch: private bounded metadata, service-only RPCs, a protected no-store health endpoint and bounded receipts on all three worker routes. See [worker health](./worker-health.md) for state semantics, security and deployment order. The new migration is not applied hosted; scheduled delivery, job backlog/retention monitoring and external alert routing remain unverified. Local/CI invocation tests are not proof of a functioning Production scheduler.
- Signed-in preview/manual-start/expiration recovery passes the limited browser flow above. The benign-stderr diagnostics regression is repaired and its browser flow is clean, but successful live AI diagnosis and actual Vercel BotID verification remain open. One redacted server `ResponseAborted` event during reconnection was not attributed; do not claim a clean full-session runtime-error gate. Customer authentication delivery, multi-project background generation, axe and responsive coverage remain open.
- Deploy and verify the private sandbox-cleanup worker and its minute schedule. Post-response delivery is verified only on the local production build; provider failure/backlog monitoring and operational tombstone retention remain open. Isolated clean migration replay, SQL invariants/type parity and the synthetic cleanup protocol now pass on GitHub's disposable Linux database; see [CI evidence](./continuous-integration.md). This is not deployed scheduler or live provider verification. Missing receipts intentionally hold quotas through the conservative observation window instead of releasing potentially live resources.
- The 2026-08-27 authenticated eight-model live matrix failed at the Gateway before tools: the configured service returns HTTP 402 (credits exhausted). A follow-up live check verifies the repaired user guidance, failed-message persistence and source retention. Restore authorized Gateway funding before rerunning successful generation/tool checks; no billing changes or purchases were made. Grok's current catalog prefix is now mapped internally while preserving existing selections. See [AI model verification and exact remaining gate](./ai-model-verification.md).
- Production Supabase variables and independent sensitive Preview/Production worker secrets are now configured in Vercel. A third local worker secret passes the local HTTP authorization smoke check. The actual Next build rejects Preview using Production data. An isolated Preview database still needs organization/cost confirmation and provisioning; credential rotation, hosted callback checks, live release checks and actual scheduler deployment remain outstanding. See [environment verification](./deployment-environments.md). No app deployment was made by this configuration work.
- Project, source, portfolio, generated activities, assessments, and progress now use authenticated cloud APIs. Hosted isolation, chat reservation, and basic live sandbox/restore checks pass. Live AI provider streaming, preview rendering, and signed-in browser workflows remain outstanding.
- Deploy and browser-verify the byte-safe command transport. The encoded server/VM path passes exact-output checks, but the live site still uses old code and historical raw commands cannot recover previously corrupted bytes. The raw SDK diagnostic intentionally still fails while the upstream issue persists; it is separate from the now-passing encoded application Unicode check. Binary/non-UTF-8 output is not a lossless text-terminal guarantee.
- The six Project blueprints now have concrete staged content, browser/CLI/service workflows, editable checks and test-only reference implementations. Their self-reported milestone checklists do not award verified progress; successful live Project rubric assessment remains open. The 18 Practice lessons, 12 Debug exercises and three React Challenges also contain tested teaching material, but deterministic Practice/Debug grading and successful live rubric assessment remain open. DSA and the 15 code-Challenge entries have concrete contracts and host-checked output. A successful compile/process exit alone must not count as proof of problem correctness; mutable learner-owned test scripts are not authoritative scoring evidence.
- Finish deployment and integration of terminal-side capture. Durable jobs, safe baselines, revision CAS, deletion tombstones, conflicting-copy preservation, fair scheduling, bounded retries, final capture before Stop and user-facing review/resolution now pass their hosted/component/live-VM tests as documented above. The checked-in minute cron route has configured worker secrets but has not been deployed. Guarded application of saved resolutions now passes hosted-VM checks and editor refresh passes component tests; signed-in browser coverage, queue/archive retention and deployed backlog/load tests remain outstanding. Saved-source submission capture is immutable and atomic, but does not by itself prove all unpersisted VM edits are durable. Do not claim database/VM/terminal state is fully synchronized.
- Deploy and browser-verify explicit recovery of the preserved legacy device database. The local preparation/download and opt-in account import flow is implemented; it must never silently claim device records for whichever account signs in first.
- Verify authenticated ownership and quotas end-to-end on **every** AI, Sandbox, file, snapshot, command, verification, and error-analysis route/tool. Shared ownership checks are implemented; passing page-level auth and unit mocks alone does not prove the paid-resource boundary.
- Complete verification that outstanding chat/snapshot work remains bound to its originating account and project, including sign-out, replacement sandboxes, and account changes.
- Verify hosted callback allowlists, actual email delivery, GitHub OAuth, and any additional provider configuration. Local config changes are not proof of hosted configuration.
- Rotate previously exposed AI Gateway/Vercel credentials and scope replacement environment variables to Preview/Production.
- Finish remaining source-capture/security checks, signed-in browser tests, preview deployment smoke tests, and monitoring before promotion. Server command reservations, auditing and kernel process-tree cleanup are now implemented and tested independently of the client log-reader limit. A clean dependency audit does not prove all runtime requirements.

## Known advisor notice

Supabase leaked-password protection was enabled and its rejection behavior verified on 2026-08-28 under the existing Pro plan, following [password security](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection). The latest security advisor has no warnings/errors. Earlier checkpoint references to that warning are historical. Current user-facing sign-in is passwordless email/OAuth; the integration scripts use random temporary passwords under bcrypt's 72-byte limit.

After the archive/import and grading-evidence migrations, advisors additionally report informational RLS-enabled-without-policies notices for private staging/imported/grading evidence and server-only cleanup tables. This is intentional default-deny defense in depth: browser roles have no table or RPC grants, and only authenticated server code uses the service role. The composite archive and grading ownership foreign-key index notices were resolved and rechecked. Do not add public policies to silence this notice. Existing performance notices concern [unused indexes](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index) and [Auth connection allocation](https://supabase.com/docs/guides/deployment/going-into-prod). No unrelated indexes or Auth settings were changed.

No deployment was made as part of these verification and recovery changes.
