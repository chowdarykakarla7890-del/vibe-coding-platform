AI_GATEWAY_API_KEY=
VERCEL_AUTH_TOKEN=
VERCEL_TEAM_ID=
VERCEL_PROJECT_ID=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3010
# Server-only secret key. Never prefix this with NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
# Server-only recovery worker authorization; use a random value of at least 32 characters.
# Configure separate sensitive values for Preview and Production before deployment.
CRON_SECRET=
